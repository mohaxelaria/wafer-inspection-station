# Milestone 2 only.
#
# Install PyTorch FIRST and separately, matched to your GPU:
#   https://pytorch.org/get-started/locally/
# RTX 50-series (Blackwell, sm_120) needs cu128 or newer:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# torch is deliberately NOT listed below, so this file can never replace your
# CUDA build with the default CPU wheel from PyPI.
numpy>=1.26
pandas>=2.0
kaggle>=1.6
